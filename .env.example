# Database
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://user:password@localhost:5432/docclassifier"

# NextAuth (si vous implémentez l'authentification)
# Générer avec: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"


GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_secret
GITHUB_ID=your_github_id
GITHUB_SECRET=your_github_secret

# CRON Job Secret (pour Vercel)
# Utilisé pour sécuriser les endpoints de nettoyage automatique
CRON_SECRET="your-cron-secret-here"

# Session Configuration
# Durée de vie des sessions en heures (défaut: 2)
SESSION_DURATION_HOURS="2"

# Storage Configuration
# Chemin de stockage temporaire des fichiers uploadés
TEMP_STORAGE_PATH="./temp/sessions"

# CORS Configuration
# Origine autorisée pour les requêtes API (en production, utiliser votre domaine)
# Exemple: ALLOWED_ORIGIN="https://votre-domaine.com"
ALLOWED_ORIGIN="*"

# Node Environment
NODE_ENV="development"
